<?xml version="1.0" encoding="UTF-8"?>
<workflow-app xmlns="uri:oozie:workflow:0.4" name="launcher-for-${workflowName}-/${olgaUser}/dealer:${dealercode}">

    <global>
        <job-tracker>${jobTracker}</job-tracker>
        <name-node>${nameNode}</name-node>
        <configuration>
            <property>
                <name>oozie.launcher.mapred.job.queue.name</name>
                <value>${launchedQueueName}</value>
            </property>
            <property>
                <name>mapred.job.queue.name</name>
                <value>${queueName}</value>
            </property>
            <property>
                <name>oozie.launcher.fs.permissions.umask-mode</name>
                <value>007</value>
            </property>
        </configuration>
    </global>

    <start to="import-cat-industry"/>

    <action name="import-cat-industry" retry-max="2" retry-interval="1" cred="${kerberos.workflow.credentials}">
        <hive2 xmlns="uri:oozie:hive2-action:0.1">
            <job-xml>${subworkflowPath}/hive-config.xml</job-xml>
            <configuration>
                <property>
                    <name>oozie.action.sharelib.for.hive</name>
                    <value>hive2</value>
                </property>
                <property>
                    <name>oozie.launcher.action.main.class</name>
                    <value>org.apache.oozie.action.hadoop.Hive2Main</value>
                </property>
            </configuration>
            <jdbc-url>${hiveServer2Url}</jdbc-url>
            <script>import-cat-industry.hql</script>
            <param>dbName=${dbName}</param>
            <param>queue=${queueName}</param>
            <param>dealercode=${dealercode}</param>
            <param>olga_hadoop_db=${olga_hadoop_db}</param>
            <param>currentMonth=${currentMonth}</param>
            <param>noOfReducers=${noOfReducers}</param>
            <param>shuffleInputBufferPercent=${shuffleInputBufferPercent}</param>
            <param>hiveServer2Url=${hiveServer2Url}</param>
        </hive2>
        <ok to="end"/>
        <error to="error"/>
    </action>

    <kill name="error">
        <message>error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>

    <end name="end"/>
</workflow-app>
